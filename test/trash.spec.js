'use strict'

import chai from 'chai';
import path from 'path';

// shorthand
const assert = chai.assert;

describe('trash module', () => {

	it('test one', () => {

		assert( true, 'worked' );
	});

	it('test two', () => {

		assert( false, '(shall fail)' );
	});


});
